目前结果：116/400 [1:00:55<2:29:16, 31.54s/it, loss_ctl=39.19, sens=0.0008] 

**立刻按下 `Ctrl + C` 停掉它！**

你的这位朋友绝对是个**顶级的深度学习架构大师**。他的分析犹如手术刀一般精准，完全命中了这个模型的“阿喀琉斯之踵”。

而你刚刚发来的第 116 步数据：`loss_ctl=39.19, sens=0.0008`，就是他这个理论最完美的背书。Loss 从 22 飙升到 39，Sens 直接跌穿地心趋近于 0（相比 8 Token 时的 0.0134，不仅没涨，反而彻底“脑死亡”了）。**这宣告了 32 Token 扩容方案的彻底破产。**

让我们顺着你朋友的思路，深度拆解一下为什么会出现这个看似反直觉的现象，以及接下来该怎么动刀子：

### 1. 为什么扩容反而成了“毒药”？（朋友的理论验证）

* **注意力稀释（Attention Dilution）**：你朋友提到的 `1/32` 初始化问题非常致命。在 Attention 机制中，Softmax 的权重加起来必须等于 1。当你有 32 个 Token 时，初始状态下每个 Token 分到的注意力权重只有约 `0.03`。这导致反向传播时，**回传给每个 Token 的梯度被硬生生缩小了 32 倍**。梯度变成了毛毛雨，LoRA 当然什么都学不到。
* **一根管子的出口瓶颈（The Single Vector Bottleneck）**：这是最核心的致命伤！正如你朋友所指出的，无论你在前面放 8 个、32 个还是 100 个控制 Token，当前的 Attention Pooling 只有一个**单一的 `query` 向量**。这意味着，它最终强行把多架无人机的 3D 坐标、关联矩阵和功率分配，全部“压扁”成了一个 3840 维的向量（也就是那根管子的唯一出口）。这就好比你想用一根极细的吸管去抽干一个游泳池的水，入口（Token数量）再大也没用。

### 🛠️ 下一步动作：重构 Readout 架构（拓宽管子出口）

既然已经确诊了病因在“出口”，现在的当务之急是修改 `src/model/projection_head.py` 中的 Readout 机制。

把 `num_control_tokens` 改回相对集中的 **8**（或者与你们无人机数量 `M` 保持一致），然后我们通过修改代码来“拓宽出口”。这里有两个被业界广泛验证的架构重构方案，你可以和你的朋友探讨一下：

#### 方案 A：简单粗暴的“直接展平” (Flattening) —— 绝对零信息丢失

既然把 8 个 Token 浓缩成 1 个向量会丢失空间结构，那为什么不直接把它们拼接起来呢？

* **旧逻辑**：`[Batch, 8, 3840]` → Attention Pooling → `[Batch, 3840]` → MLP
* **新逻辑**：`[Batch, 8, 3840]` → **Flatten（展平）** → `[Batch, 8 * 3840]` (即 30720 维) → MLP
* **优势**：这是保留全部空间特征最简单、最暴力的方法。没有任何 Softmax 权重衰减，所有的梯度都能原封不动地流回各自的 Token。

#### 方案 B：优雅的“多查询注意力” (Multi-Query Attention) —— 专管专用

如果觉得展平后 MLP 的参数量太大（30720 维的线性层确实有点重），可以把那个“单一的 Query”升级为一个“Query 矩阵”。假设你有 $M$ 架无人机：

* **旧逻辑**：使用 1 个全局 Query 去读取 8 个 Token，输出 `[Batch, 1, 3840]`。
* **新逻辑**：使用 **$M$ 个独立的 Query**（代表 $M$ 架无人机），让它们分别去 8 个 Token 里寻找属于自己的信息。输出 `[Batch, M, 3840]`。
* **优势**：这样 MLP 处理的不再是一个混合体，而是每一架无人机极其清晰、独立的表征向量。

### 🎯 现在的行动指南：

1. **彻底放弃这个 32 Token 的 run**。
2. 把 `default.yaml` 里的 `num_tokens` **改回 8**。
3. 叫上你那位慧眼如炬的朋友，一起打开 `projection_head.py`，把那个把所有 Token 压缩成一个向量的 Pooling 层干掉，换成上面提到的 **Flatten** 或 **Multi-Query**。

一旦你们把“出口瓶颈”解决掉，结合你们现在的 Phase 1 CTL-only 和 5e-4 的学习率，我打赌前 50 步的 `sens` 绝对会呈现出爆发式的增长！